# Next-beta handoff — 2026-07-18

This handoff parks the checked GitHub issue implementation work for resumption on 2026-07-19. Repository and GitHub facts below were rechecked at the end of the session; do not infer newer release or validation state from issue labels alone.

## Resume point

- Repository branch: `agent/modsuite-0.3.3-beta`
- Exact implementation commit: `a3dc882` (`Generate supported known entity identities (#60)`); the only intended later branch change is this documentation-only handoff commit.
- Worktree: clean
- Remote tracking branch: up to date
- No issue-24 implementation branch was created.
- No DLLs were installed into the game, and no tag or GitHub release was created or changed.

Start tomorrow with:

```powershell
git checkout agent/modsuite-0.3.3-beta
git pull --ff-only
git status --short
git log -5 --oneline
```

Expected first log entries:

```text
<documentation-only handoff commit>
a3dc882 Generate supported known entity identities (#60)
7a5de22 Centralize typed registry resolution (#59)
5d9c6c2 Record reverse-engineering evidence strength (#58)
dd70053 Centralize game lifecycle generation (#57)
5a8e1a5 Verify native mutation postconditions (#56)
```

## Completed in the checked #22–#41 set

The following work is present in `agent/modsuite-0.3.3-beta`:

| Issue | Integration commit / PR | State in beta | Remaining gate |
|---|---|---|---|
| #22 shared lifecycle readiness and generation | `dd70053` / PR #57 | Implemented | Interactive save-switch, reset/NG+, and scene-transition validation |
| #23 lifecycle-aware typed registry resolver | `7a5de22` / PR #59 | Implemented | Interactive registry population/lifecycle validation |
| #25 authoritative queue capacity | `75da98a` / PR #45 | Implemented | Interactive capacity-change and manual/automation reservation validation |
| #29 generated known-entity identities | `a3dc882` / PR #60 | Implemented | Runtime registry population remains authoritative |
| #30 machine-readable native contracts | `91393e5` / PR #50 | Implemented | Re-audit when installed assembly hashes change |
| #31 evidence strength | `5d9c6c2` / PR #58 | Implemented | Interactive diagnostics review |
| #36 native mutation postconditions | `5a8e1a5` / PR #56 | Implemented | Interactive mutation/UAT scenarios |

GitHub still shows these issues as open because their implementation PRs target the non-default next-beta branch. Do not reimplement them. Close them through the eventual promotion to `main`, or update issue state explicitly as part of that promotion workflow.

Related beta ancestry also contains the queue performance A/B harness and history, progression-triggered Auto Buy refresh, threshold parking, headless integration/P1 regression gates, Mentor progression gates, and Scholar Concept exclusion.

## Current verification evidence

Current-tree verification after the final #29 changes:

- Generated identity verification: 16 entries reproduced from the canonical mapping.
- Portable/headless tests: 443 passed, 0 failed, 0 skipped.
- Installed-game contract tests: 19 passed, 0 failed, 0 skipped.
- Real-reference Release builds: OrbAutomata, OrbModConfig, and OrbMentor all passed with 0 warnings and 0 errors.
- Production line coverage floors passed:
  - OrbAutomata: 74.41% (floor 70%)
  - OrbMentor: 69.85% (floor 64%)
  - OrbModConfig: 24.33% (floor 24%)
  - OrbModding.Common: 84.13% (floor 83%)
  - Overall production: 69.93% (floor 65%)
- PR #59 and PR #60 passed all four CI jobs before squash merge.
- Runtime-contract audits returned GO for #23 and #29.

The latest exact coverage artifact is under:

```text
artifacts/coverage/issue29-final-negative-fixtures-20260718/
```

Portable and installed verification commands:

```powershell
dotnet test tests/OrbModding.Tests/OrbModding.Tests.csproj -p:UseGameStubs=true
tools/test-modsuite.ps1 -GameRoot "C:\Program Files (x86)\Steam\steamapps\common\Orb of Creation"
```

## Tomorrow’s next implementation: issue #24

Start #24, “Introduce a shared coalescing gameplay invalidation bus,” from the clean integration branch:

```powershell
git checkout -b agent/issue-24-invalidation-bus
gh issue view 24
```

Read before changing behavior:

- `docs/plans/performance-suite.md`
- `src/OrbModding.Common/GameLifecycleMonitor.cs`
- `src/OrbAutomata/AutoBuyLifecycleSignal.cs`
- Auto Buy candidate/index dirty-resource paths
- Automata, Mentor, and Mod Config plugin lifecycle entry points

Required #24 outcomes:

1. Common change kinds cover lifecycle, progression, resource quantity/rate, queue, inventory, registry, and configuration.
2. Equivalent burst events coalesce by a stable key.
3. Every event carries the lifecycle generation; stale events are discarded.
4. Consumers can invalidate targeted dependencies without mandatory full scans.
5. Pending length, dropped/coalesced counts, and bounded processing work are observable.
6. Tests cover bursts, ordering, late/stale events, targeted dependencies, and lifecycle reset.

Implementation constraints:

- Keep publishing and draining on the Unity main thread.
- Do not perform reflection discovery, registry rebuilds, full sorts, or native mutation inside bus callbacks.
- Make delivery operation-bounded and resumable; do not create an unbounded event queue.
- Coalescing must not erase a broader invalidation with a narrower later event.
- Lifecycle/reset events must dominate older-generation entity events.
- The game remains authoritative at final availability, cost, queue, completion, and mutation checks.

The read-only #24 design agent was intentionally interrupted when work was parked. It made no edits and produced no implementation that should be recovered.

## Recommended remaining sequence

After #24, use separate issue branches and PRs back to `agent/modsuite-0.3.3-beta`:

1. #27 structured automation decisions and rejection codes.
2. #28 unified feature capability/health/blocked status.
3. #37 bounded circuit breakers, using #24, #27, #28, and existing #36 postconditions.
4. #26 admission-policy/native-adapter separation and #35 action-family ownership isolation; they can be reviewed independently.
5. #38 reusable lifecycle state-machine scenarios.
6. #39 sanitized structured event replay fixtures, after #24 and #38.
7. #40 enforceable per-module performance/starvation budgets.
8. #32 versioned configuration schemas and migrations.
9. #34 unsaved/external three-way conflict handling, then #33 presets with staged diffs.
10. #41 compatibility/runtime health report after #27, #28, #30, and the later health/performance providers exist.

#54, adaptive bounded Auto Buy mutation bursts, is the separate follow-up for reducing initial queue saturation time. Keep it distinct from #24: use the existing A/B compatibility harness and historical performance reports to prove throughput gains without weakening reserve, queue, postcondition, or Mentor XP guarantees.

## Promotion and release boundary

The user’s intended flow is:

1. Validate the current beta in game.
2. Promote the validated beta to `main` and release it.
3. Continue building the next validation batch on the next-beta integration branch.

Open draft PR #43 targets `main` from `agent/automata-0.8.1-beta` at older commit `b29afbe`. It does **not** contain PRs #56–#60 and must not be merged as the current promotion PR. After user validation, create or retarget a promotion PR from the exact validated `agent/modsuite-0.3.3-beta` commit, then perform the required release review: commit, tag, versions, supported-plugin allowlist, archive entries, checksums, tests, and prerelease/stable status.

Do not install DLLs, create tags, or publish/replace releases without the user’s explicit instruction at that time.

## Known validation gaps to preserve

- Headless tests simulate policy, scheduling, lifecycle, registry, queue, and performance behavior but do not prove Unity presentation or actual save-transition behavior.
- Computer-use testing remains UAT, not the primary automated E2E layer.
- #22/#23/#25/#29/#36 still need their documented interactive gates on the installed game.
- The generated mappings and contract manifest support only the audited installed assembly hashes; changed hashes require renewed inspection.
- Current queue performance evidence is deterministic simulation/A-B evidence. Actual Steam Deck/Proton timing and the user’s high-capacity save remain UAT evidence.
